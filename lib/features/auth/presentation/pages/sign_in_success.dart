import 'package:flutter/material.dart';

class SignInSuccess extends StatelessWidget {
  const SignInSuccess({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("Successful Sign In"));
  }
}
